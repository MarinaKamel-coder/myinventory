import 'package:flutter/material.dart';

class CartonListTile extends StatelessWidget {
  const CartonListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}
