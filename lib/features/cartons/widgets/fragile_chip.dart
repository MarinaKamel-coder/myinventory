import 'package:flutter/material.dart';

class FragileChip extends StatelessWidget {
  const FragileChip({super.key});

  @override
  Widget build(BuildContext context) => const Chip(label: Text('Fragile'));
}
