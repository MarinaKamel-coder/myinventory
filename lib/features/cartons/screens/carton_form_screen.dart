import 'package:flutter/material.dart';

class CartonFormScreen extends StatelessWidget {
  const CartonFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
