import 'package:flutter/material.dart';

class ScanHowToCard extends StatelessWidget {
  const ScanHowToCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}
